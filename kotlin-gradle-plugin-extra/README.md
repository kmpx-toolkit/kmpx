# Kotlin Gradle Plugin Extra

Basic extra functionality (mostly static typing) built on top of the official Kotlin Gradle Plugin.

This module itself _is not_ a Gradle plugin, but can be consumed from Gradle plugins.
